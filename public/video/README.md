# Hero video (optional)

Drop `01.mp4` (H.264, muted, a few seconds, loops cleanly; `01.webm` also
accepted) into this folder and the homepage hero plays it behind image 01,
which stays as the poster. It is skipped automatically for viewers who ask
for reduced motion or have data-saver on. Keep it under ~3 MB.
